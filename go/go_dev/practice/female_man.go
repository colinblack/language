package main

const (
	man    = 1
	female = 2
)

//定义两个常量Man=1和Fmale=2, 获取当前时间的秒数，如果能被Female整除，则在终端打印female，否则打印man
func femaleMan() {

	/* 	second := time.Now().Unix
	   	if int(second)%female == 0 {
	   		fmt.Println("female=", female)
	   	} else {
	   		fmt.Println("man=", man)
	   	}
	*/
}
